'use strict';

app.directive('beaware', function($location) {
  return {
    templateUrl: '../views/home.html',
    link: function(scope, elem) {
      scope.state = {

      };
    },
    controller: function() {

    }
  };
});
